version: '3.8'
services:
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    ports: ["8081:8080"]
    environment:
      SWAGGER_JSON: /openapi.yaml
    volumes:
      - ../../docs/api/openapi.yaml:/openapi.yaml